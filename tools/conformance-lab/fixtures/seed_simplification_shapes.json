[
  {
    "id": "simp/unsorted_mul",
    "kind": "simplification_case",
    "case": "unsorted_mul",
    "note": "Mul factor order: oracle sorts non-numeric factors (cos before tan)."
  },
  {
    "id": "simp/same_base_pows",
    "kind": "simplification_case",
    "case": "same_base_pows",
    "note": "Same-base Pow factors merge arithmetically: x**2 * x**3 -> x**5."
  },
  {
    "id": "simp/nested_pow",
    "kind": "simplification_case",
    "case": "nested_pow",
    "note": "Integer outer exponent flattens: (x**2)**3 -> x**6."
  },
  {
    "id": "simp/exp_products",
    "kind": "simplification_case",
    "case": "exp_products",
    "note": "exp(x)*exp(y) merges to exp(x + y)."
  },
  {
    "id": "simp/symbolic_exp_pows",
    "kind": "simplification_case",
    "case": "symbolic_exp_pows",
    "note": "Symbolic exponents do NOT merge at construction (sign safety)."
  },
  {
    "id": "simp/radical_constant",
    "kind": "simplification_case",
    "case": "radical_constant",
    "note": "sqrt(8) -> 2*sqrt(2) with sqrt str rendering."
  },
  {
    "id": "simp/negative_half",
    "kind": "simplification_case",
    "case": "negative_half",
    "note": "1/sqrt(2) constructs Pow(2, -1/2); str renders sqrt(2)/2 via radsimp form."
  },
  {
    "id": "simp/numeric_coeff_add",
    "kind": "simplification_case",
    "case": "numeric_coeff_add",
    "note": "Numeric-coefficient Add: stored args order vs printed order pinned."
  },
  {
    "id": "simp/rational_function",
    "kind": "simplification_case",
    "case": "rational_function",
    "note": "Registered: the earlier \"div over-cancellation\" diagnosis was WRONG - construction preserves the fraction; the latex printer dropped the denominator (q==1 branch discarded denom_terms). Printer fixed. DEFERRED - div over-cancellation: shell cancels (x**2-1)/(x+1) to x-1 at construction; oracle keeps the fraction (cancellation is domain-unsound for x = -1 and not performed by upstream at construction). Deepest WS07 finding - requires div-path change with wide test blast radius. Univariate rational function."
  },
  {
    "id": "simp/trig_identity",
    "kind": "simplification_case",
    "case": "trig_identity",
    "note": "Registered: Add printer ordering implemented (function class ranks + negative-Pow descending base). DEFERRED - Add printer term ordering for this shape pending upstream sort_key study (trig_identity per-term latex placement is FIXED; only the sin/cos term order remains; multivariate_rational needs the negative-Pow descending-base rule 1/y before 1/x). Registered after function-power latex placement + ordering verification.DEFERRED - Add printer ordering for function/negative-power terms pending upstream sort_key study. Construction must NOT fold the Pythagorean identity (WS07)."
  },
  {
    "id": "simp/multivariate_rational",
    "kind": "simplification_case",
    "case": "multivariate_rational",
    "note": "Registered: Add printer ordering implemented (function class ranks + negative-Pow descending base). DEFERRED - Add printer term ordering for this shape pending upstream sort_key study (trig_identity per-term latex placement is FIXED; only the sin/cos term order remains; multivariate_rational needs the negative-Pow descending-base rule 1/y before 1/x). Registered after function-power latex placement + ordering verification.DEFERRED - Add printer ordering for function/negative-power terms pending upstream sort_key study. Multivariate rational: apart refuses, printers/other functions do not."
  }
]